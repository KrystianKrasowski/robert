# add_subdirectory(atmega88pa-stub)
# add_subdirectory(dualshock2)
# add_subdirectory(motion)

if(${ROBERT_ENABLE_TESTS})
    target_compile_definitions(avrhal INTERFACE __AVR_ATmega88PA__)
    include(CTest)
    enable_testing()
    add_subdirectory(simple)
endif()